# Decap CMS 配置文件
# 文档: https://decapcms.org/docs/

backend:
  name: github
  repo: Pheglovog/pheglovog-site
  branch: main
  # 社区提供的免费 OAuth 代理服务
  base_url: https://sveltia-cms-auth.netlify.app

# 本地开发时使用 (运行 npx decap-server)
# local_backend: true

# 媒体文件配置
media_folder: "static/images/uploads"
public_folder: "/images/uploads"

# 站点 URL
site_url: https://pheglovog.github.io/pheglovog-site/
display_url: https://pheglovog.github.io/pheglovog-site/

# 语言设置
locale: "zh_Hans"

# 搜索功能
search: true

# Slug 配置
slug:
  encoding: "ascii"
  clean_accents: true
  sanitize_replacement: "-"

# 内容集合定义
collections:
  # 博客文章
  - name: "posts"
    label: "📝 博客文章"
    label_singular: "文章"
    folder: "content/posts"
    create: true
    slug: "{{slug}}"
    preview_path: "posts/{{slug}}"
    extension: "md"
    editor:
      preview: true
    sortable_fields: ["date", "title"]
    view_filters:
      - label: "草稿"
        field: draft
        pattern: true
      - label: "已发布"
        field: draft
        pattern: false
    view_groups:
      - label: "年份"
        field: date
        pattern: \d{4}
      - label: "分类"
        field: categories
    fields:
      - label: "标题"
        name: "title"
        widget: "string"
        required: true
      - label: "发布日期"
        name: "date"
        widget: "datetime"
        date_format: "YYYY-MM-DD"
        time_format: false
        format: "YYYY-MM-DD"
        required: true
      - label: "分类"
        name: "categories"
        widget: "relation"
        collection: "categories"
        search_fields: ["name"]
        value_field: "name"
        display_fields: ["name"]
        multiple: true
        required: false
      - label: "标签"
        name: "tags"
        widget: "relation"
        collection: "tags"
        search_fields: ["name"]
        value_field: "name"
        display_fields: ["name"]
        multiple: true
        required: false
      - label: "草稿"
        name: "draft"
        widget: "boolean"
        default: false
        required: false
      - label: "摘要"
        name: "summary"
        widget: "text"
        required: false
        hint: "文章摘要，不填则自动截取"
      - label: "封面图片"
        name: "cover"
        widget: "image"
        required: false
        allow_multiple: false
      - label: "内容"
        name: "body"
        widget: "markdown"
        required: true

  # 分类管理
  - name: "categories"
    label: "📁 分类"
    label_singular: "分类"
    folder: "content/categories"
    create: true
    slug: "{{slug}}"
    identifier_field: "name"
    fields:
      - label: "分类名称"
        name: "name"
        widget: "string"
        required: true
      - label: "描述"
        name: "description"
        widget: "text"
        required: false
      - label: "排序权重"
        name: "weight"
        widget: "number"
        default: 0
        required: false

  # 标签管理
  - name: "tags"
    label: "🏷️ 标签"
    label_singular: "标签"
    folder: "content/tags"
    create: true
    slug: "{{slug}}"
    identifier_field: "name"
    fields:
      - label: "标签名称"
        name: "name"
        widget: "string"
        required: true
      - label: "描述"
        name: "description"
        widget: "text"
        required: false

  # 项目展示
  - name: "projects"
    label: "🚀 项目"
    label_singular: "项目"
    folder: "content/projects"
    create: true
    slug: "{{slug}}"
    fields:
      - label: "项目名称"
        name: "title"
        widget: "string"
        required: true
      - label: "描述"
        name: "description"
        widget: "text"
        required: true
      - label: "GitHub 链接"
        name: "github"
        widget: "string"
        required: false
      - label: "演示链接"
        name: "demo"
        widget: "string"
        required: false
      - label: "技术栈"
        name: "tags"
        widget: "list"
        required: false
      - label: "排序权重"
        name: "weight"
        widget: "number"
        default: 0
        required: false
      - label: "详细内容"
        name: "body"
        widget: "markdown"
        required: false

  # 页面管理
  - name: "pages"
    label: "📄 页面"
    files:
      - label: "关于页面"
        name: "about"
        file: "content/about/_index.md"
        fields:
          - label: "标题"
            name: "title"
            widget: "string"
          - label: "内容"
            name: "body"
            widget: "markdown"
